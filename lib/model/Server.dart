import 'package:flutter/material.dart';

class Server {
  String name;
  String address;
  Color color;
  bool premium;
  String token;

  Server(this.name, this.address, this.color, this.premium, this.token);


}